import 'package:flutter/material.dart';

class flower extends StatelessWidget {
  const flower({super.key});
  static const String route = '/flower';

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
