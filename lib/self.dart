import 'package:flutter/material.dart';

class self extends StatelessWidget {
  const self({super.key});
  static const String route = '/self';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
