import 'package:flutter/material.dart';

class track extends StatelessWidget {
  const track({super.key});
  static const String route = '/track';


  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
