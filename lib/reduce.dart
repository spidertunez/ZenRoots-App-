import 'package:flutter/material.dart';

class reduce extends StatelessWidget {
  const reduce({super.key});
  static const String route = '/reduce';


  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
